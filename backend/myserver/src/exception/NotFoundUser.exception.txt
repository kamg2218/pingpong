import { HttpException, HttpStatus } from "@nestjs/common";

export class NotFoundUserException extends HttpException {
    constructor(private email : string) {
        super('NotFoundUser', HttpStatus.UNAUTHORIZED);
        this.message = "signup"
    }

    get Email() {
        return this.email;
    }
}